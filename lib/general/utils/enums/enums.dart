enum UserRoutingEnum { userScreen, userDetailWidget }

enum PropertiesRoutingEnum { propertiesScreen, propertiesDetailWidget }

enum UploadedByAdminRoutingEnum {
  uploadedViewScreen,
  uploadedDetailsScreen,
  uploadingWidgetScreen,
  editUploadedWidgetScreen,
}

enum ReportsAndIssuesEnum { reportsAndIssuesScreen, reportsUserDetailWidget }

enum ConstructionStatus { newLaunch, readyToMove, underConstruction }

enum SelectedCategory {
  house,
  apartments,
  landsPlots,
  commercial,
  coWorkingSpace,
  pGGuestHouse
}

enum SelectedType { sell, rent }

enum SelectedFurnisher { furnished, semiFurnished, unFurnished }

enum SelectedListedBy { builder, dealer, owner }

enum SelectedFacing {
  east,
  north,
  west,
  south,
  northEast,
  northwest,
  southEast,
  southWest,
}
